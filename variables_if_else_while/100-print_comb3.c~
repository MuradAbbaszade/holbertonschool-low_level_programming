#include <stdio.h>

/**
  * main - Prints the numbers since 0 to 9
  *
  * Return: Always (Success)
  */
int main(void)
{
	char n;
	char m;

	for (n = '0'; n <= '9'; n++)
	{
	    for (m = '1'; m <= '9'; m++)
	    {
	        	if (n < m)
			{
	        	    putchar(n);
	        	    putchar(m);
		            putchar(' ');
	        	}
	    }
	}
	

	putchar('\n');

	return (0);
}

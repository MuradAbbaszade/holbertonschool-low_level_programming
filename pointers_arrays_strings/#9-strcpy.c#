#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strcpy - check the code
 * @dest:input
 * @src:input
 * Return: Always 0.
 */
char *_strcpy(char *dest, char *src)
{
int i;
int len = 	int i;

	i = strlen(src);

	while (i >= 0)
	{
		*(dest + i) = *(src + i);
		i--;
	}
	return (dest);
 for (i=strlen(src)i >= 0)
	{
		*(dest + i) = *(src + i);
		i--;
	}
return (dest);
}

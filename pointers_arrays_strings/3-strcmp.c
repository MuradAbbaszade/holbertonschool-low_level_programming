#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strcmp - check the code
 * @s1:input
 * @s2:input
 * Return: Always 0.
 */
int _strcmp(char *s1, char *s2)
{
return (strcmp(s1,s2));
}

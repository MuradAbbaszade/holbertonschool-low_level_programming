#include <stdio.h>
#include <string.h>
/**
 *  - check the code
 *
 * Return: Always 0.
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
unsigned int i;
char *result = dest;
for (i = 0 ; i < n ; i++)
*(dest + i) = *(src + i);
return (result);
}

#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * rev_string - check the code
 * @s:input
 * Return: Always 0.
 */
void rev_string(char *s)
{
int len = strlen(s);
int i;
for(i=0;i<(len*2)-1;i++){
if(i<len-1) s++;
else putchar(*s--);
}
}

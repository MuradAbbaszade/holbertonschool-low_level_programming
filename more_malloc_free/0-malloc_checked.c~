#include <stdio.h>
#include <stdlib.h>
/**
 * malloc_checked - a
 * @b: t
 * Return: Nothing.
 */
void *malloc_checked(unsigned int b)
{
int *arr;
arr = malloc(b * sizeof(int));
if (arr == NULL)
exit(98);
return (arr);
}
